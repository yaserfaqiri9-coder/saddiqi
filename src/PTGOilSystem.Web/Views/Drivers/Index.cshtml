@model IEnumerable<Driver>
@{
    ViewData["Title"] = UiText.T(Context, "راننده‌ها", "Drivers");
    var drivers = Model.ToList();
    var query = ViewData["q"]?.ToString();
    var returnUrl = $"{Context.Request.Path}{Context.Request.QueryString}";
    var hasFilters = ViewData["isActive"] != null;
    var hasSearch = !string.IsNullOrWhiteSpace(query) || hasFilters;

    string TextOrDash(string? value) => string.IsNullOrWhiteSpace(value) ? "-" : value.Trim();
}

<div class="ak-list-page">
    <partial name="~/Views/Shared/Components/Ak/_AkPageHeader.cshtml"
             model="@((UiText.T(Context, "راننده‌ها", "Drivers"), true,
                       UiText.T(Context, "راننده جدید", "New driver"), "bi-plus-lg",
                       (string?)Url.Action("Create", new { returnUrl }), (string?)null,
                       (string?)null, (string?)null, (string?)null))" />

    @{
        var driversIsActive = ViewData["isActive"] is bool dIsActive ? (dIsActive ? "true" : "false") : null;
        var driversFilter = new AkSearchFilterModel(
            "q", query,
            UiText.T(Context, "جستجو در نام، جواز، تذکره یا تلفن...", "Search name, license, national ID, or phone..."),
            new List<AkFilterDefinition>
            {
                new("isActive", UiText.T(Context, "وضعیت", "Status"), "bool",
                    new List<AkFilterOption>
                    {
                        new("true", UiText.T(Context, "فعال", "Active")),
                        new("false", UiText.T(Context, "غیرفعال", "Inactive"))
                    }, driversIsActive)
            });
    }
    <partial name="~/Views/Shared/_AkSearchFilter.cshtml" model="driversFilter" />

    <div class="ak-list">
        <div class="ak-table-wrap">
            <table class="ak-table" aria-label="@UiText.T(Context, "لیست راننده‌ها", "Driver list")">
                <thead>
                    <tr>
                        <th class="ak-col-check"><input type="checkbox" class="ak-check" aria-label="@UiText.T(Context, "انتخاب همه", "Select all")" /></th>
                        <th class="ak-col-grow">@UiText.T(Context, "نام", "Name")</th>
                        <th>@UiText.T(Context, "جواز", "License")</th>
                        <th>@UiText.T(Context, "تذکره", "National ID")</th>
                        <th>@UiText.T(Context, "تلفن", "Phone")</th>
                        <th class="ak-col-spacer"></th>
                        <th>@UiText.T(Context, "وضعیت", "Status")</th>
                        <th class="ak-col-actions"></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in drivers)
                    {
                        <tr data-href="@Url.Action("Details", new { id = item.Id })">
                            <td class="ak-col-check"><input type="checkbox" class="ak-check" aria-label="@UiText.T(Context, "انتخاب", "Select")" /></td>
                            <td class="ak-col-grow"><partial name="~/Views/Shared/Partials/_PersonCell.cshtml" model="@((item.FullName, (string?)Url.Action("Details", new { id = item.Id }), (string?)null))" /></td>
                            <td>@TextOrDash(item.LicenseNumber)</td>
                            <td>@TextOrDash(item.NationalId)</td>
                            <td class="ak-num">@TextOrDash(item.Phone)</td>
                            <td class="ak-col-spacer"></td>
                            <td>
                                <span class="ak-status @(item.IsActive ?"is-active" : "is-inactive")">
                                    @(item.IsActive ? UiText.T(Context, "فعال", "Active") : UiText.T(Context, "غیرفعال", "Inactive"))
                                </span>
                            </td>
                            <td class="ak-col-actions">
                                <div class="dropdown ak-row-menu">
                                    <button type="button" class="ak-row-menu-btn" data-bs-toggle="dropdown" aria-expanded="false" aria-label="@UiText.T(Context, "عملیات", "Actions")">
                                        <i class="bi bi-three-dots-vertical" aria-hidden="true"></i>
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-end">
                                        <li><a class="dropdown-item" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye" aria-hidden="true"></i>@UiText.T(Context, "جزئیات", "Details")</a></li>
                                        <li><a class="dropdown-item" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil" aria-hidden="true"></i>@UiText.T(Context, "ویرایش", "Edit")</a></li>
                                        <li><hr class="dropdown-divider" /></li>
                                        <li><partial name="~/Views/Shared/Partials/_ToggleActiveButton.cshtml" model="@(("Drivers", item.Id, item.IsActive, "dropdown-item ak-danger", (string?)returnUrl))" /></li>
                                    </ul>
                                </div>
                            </td>
                        </tr>
                    }
                    @if (!drivers.Any())
                    {
                        <tr>
                            <td colspan="8">
                                <div class="ak-empty"><i class="bi bi-person-badge" aria-hidden="true"></i>@UiText.T(Context, "رکوردی یافت نشد", "No records found")</div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @{ ViewData["ShownCount"] = drivers.Count; }
        <div class="ak-pager">@await Html.PartialAsync("_PagedListFooter")</div>
    </div>
</div>
